# --- Environment Usage Guide ---
#
# This project can be run in two modes: with the external database on Neon
# or with a local database managed by Docker. Copy this file to .env and
# fill in the appropriate variables for the mode you want to use.
#
# -------------------------------------------
#
# 🚀 NEON MODE (EXTERNAL DATABASE)
#
# This configuration connects the app to the database on Neon.
# The local database container ('db') is not needed and should not be started.
#
# 1. Activate this mode:
#    cp .env.example .env (and fill in DATABASE_URL)
#
# 2. Start ONLY the web service:
#    docker-compose up --build web
#
# -------------------------------------------
#
# 💻 LOCAL MODE (DOCKER DATABASE)
#
# This configuration uses a Postgres database running in a local Docker container.
# Both the 'web' and 'db' services must be started.
#
# 1. Activate this mode:
#    cp .env.example .env (and ensure DATABASE_URL is commented out)
#
# 2. Start both services:
#    docker-compose up --build
#
# -------------------------------------------

# --- General Settings ---
RAILS_ENV=development


# --- OPTION 1: Neon DB (External) ---
# Use this variable when connecting to a remote database like Neon or Render.
DATABASE_URL="postgres://USER:PASSWORD@HOST/DATABASE?sslmode=require"


# --- OPTION 2: Local Docker DB ---
# Use these variables when running the local Docker database.
# IMPORTANT: When using this option, the DATABASE_URL above must be empty or commented out.

# Variables for the Postgres 'db' container
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=arthafolio_local

# Variables for the Rails 'web' container
PGHOST=db
PGUSER=postgres
PGPASSWORD=password
PGDATABASE=arthafolio_local